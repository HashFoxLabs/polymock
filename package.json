{
  "name": "polymock-monorepo",
  "private": true,
  "scripts": {
    "build": "npm install --prefix landing && npm run --prefix landing build",
    "build:landing": "npm install --prefix landing && npm run --prefix landing build",
    "build:frontend": "npm install --prefix landing && npm run --prefix landing build",
    "dev:landing": "npm run --prefix landing dev",
    "dev:frontend": "npm run --prefix frontend dev"
  }
}

